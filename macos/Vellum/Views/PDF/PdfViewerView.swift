import SwiftUI

// STUB — replaced by the pdf-ui module (see macos/specs/SPECS-pdf-viewing.md and
// SPECS-annotations.md). PDFKit-based viewer: continuous scroll, zoom, text
// selection → SelectionPopover, HighlightLayer + StickyNoteOverlay overlays,
// note-placement mode, context menu, page-text extraction feed to AiStore,
// locator + snapshot registration. Signature frozen: environment-driven.
struct PdfViewerView: View {
    var body: some View {
        Color.clear
    }
}
