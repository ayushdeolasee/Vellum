import SwiftUI

// STUB — replaced by the chrome module (see macos/specs/SPECS-pdf-viewing.md
// "TabBar"). Signature frozen: environment-driven.
struct TabBarView: View {
    var body: some View {
        Color.clear.frame(height: 0)
    }
}
