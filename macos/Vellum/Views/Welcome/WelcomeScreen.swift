import SwiftUI

// STUB — replaced by the shell module (see macos/specs/SPECS-app-shell.md
// "Welcome screen"). Recent files, saved webpages, open buttons, URL entry.
// Signature frozen: environment-driven.
struct WelcomeScreen: View {
    var body: some View {
        Color.clear
    }
}
