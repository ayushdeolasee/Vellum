import SwiftUI

// STUB — replaced by the chrome module (see macos/specs/SPECS-annotations.md
// "Annotation sidebar"). Signature frozen: environment-driven.
struct AnnotationSidebar: View {
    var body: some View {
        Color.clear
    }
}
