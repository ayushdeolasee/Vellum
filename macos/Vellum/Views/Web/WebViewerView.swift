import SwiftUI

// STUB — replaced by the web module (see macos/specs/SPECS-web.md). WKWebView
// reading mode with content-script bridge (virtual pages, text anchors, note
// popovers, visibility tracking). Signature frozen: environment-driven.
struct WebViewerView: View {
    var body: some View {
        Color.clear
    }
}
