import SwiftUI

// STUB — replaced by the AI module (see macos/specs/SPECS-ai.md). Chat panel:
// header, settings, message list (markdown+math), composer with push-to-talk,
// TTS. Signature frozen: environment-driven.
struct AiPanel: View {
    var body: some View {
        Color.clear
    }
}
